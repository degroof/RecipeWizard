<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingTop="0dp"
    android:paddingEnd="0dp"
    android:paddingBottom="0dp">

    <ImageView
        android:id="@+id/drag_handle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/drag_to_reorder_description"
        android:padding="8dp"
        android:src="@drawable/ic_drag_handle"
        app:tint="?android:attr/textColorSecondary" />

    <TextView
        android:id="@+id/text_view_block_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textAppearance="?attr/textAppearanceBody1"
        tools:text="This is a line of recognized text from the image." />

    <ImageButton
        android:id="@+id/button_remove_text_block"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/remove_text_block_description"
        android:padding="8dp"
        android:src="@drawable/ic_clear"
        app:tint="?android:attr/textColorSecondary" />

</LinearLayout>
